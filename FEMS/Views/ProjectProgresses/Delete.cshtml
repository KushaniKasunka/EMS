@model FEMS.Database.project_progress

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Project Progresses</h4>
    <hr />
    <dl class="dl-horizontal">
        
        <dt>
            @Html.DisplayNameFor(model => model.no_of_tasks)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.no_of_tasks)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.days_remaining)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.days_remaining)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.no_of_finished_tasks)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.no_of_finished_tasks)
        </dd>
    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(model => model.progress_id)
        @Html.HiddenFor(model => model.no_of_finished_tasks)
        @Html.HiddenFor(model => model.no_of_tasks)
        @Html.HiddenFor(model => model.days_remaining)
        @Html.HiddenFor(model => model.project_id)

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
